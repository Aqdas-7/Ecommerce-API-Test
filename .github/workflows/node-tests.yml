name: Run Jest API Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: 🔄 Checkout code
      uses: actions/checkout@v3

    - name: 🟢 Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'  # Or use your project’s Node version

    - name: 📦 Install dependencies
      run: npm install

    - name: ✅ Run Jest tests
      run: npx jest --coverage
